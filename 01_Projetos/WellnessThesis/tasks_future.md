# 🩺 Future Tasks: WellnessThesis (Health Blog)

## 1. Fix TypeScript h1 Error
**Prompt**: "Resolve 'Cannot find name h1' in \`src/app/post/[slug]/page.tsx\`. Check the \`tsconfig.json\` for correct JSX configuration and ensure React types are properly loaded. Add \`import React from 'react';\` if needed as a temporary fix."

## 2. Affiliate Ads & CTA
**Prompt**: "Implement the Affiliate Marketing engine. 1. Update the Post model to include an array of \`ad_slots\` (HTML/Script snippets). 2. Add a \`CTA_Link\` field for each post. 3. Update the frontend template to render these ads in strategic positions (top of post, mid-content, bottom)."

## 3. SEO & Performance
**Prompt**: "Optimize the dynamic routes for SEO. Implement \`generateMetadata\` for the slug page to include OpenGraph tags and Meta Description extracted from the post content."
